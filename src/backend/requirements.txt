# Production dependencies for StagePi backend

# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3

# CORS middleware
python-multipart==0.0.6

# GStreamer CLI tools (system package, managed via supervisor)
# Install via: sudo apt-get install gstreamer1.0-tools gstreamer1.0-plugins-base gstreamer1.0-plugins-good gstreamer1.0-alsa
# Streams are managed as individual supervisor programs using gst-launch-1.0
